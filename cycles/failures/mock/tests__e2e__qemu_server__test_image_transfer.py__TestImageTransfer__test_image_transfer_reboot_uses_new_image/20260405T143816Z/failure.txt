self = <tests.e2e.qemu_server.test_image_transfer.TestImageTransfer object at 0xfc842bed74d0>
qemu_container = {'api_key': 'test-key', 'base_url': 'http://localhost:50111', 'container': <Container: e252036b5dc5>, 'container_name': 'test-qemu-shared-06b952e4', ...}
minio_service = {'access_key': 'minioadmin', 'bucket': 'test-images', 'host_url': 'http://localhost:51439', 'internal_url': 'http://lab-mcp-minio-2c5c52a5:9000', ...}
test_image = '/mnt/second_disk/openbmc-builder-data/test_images/obmc-phosphor-image-evb-ast2600-pppd.static.mtd'

    async def test_image_transfer_reboot_uses_new_image(
        self, qemu_container, minio_service, test_image
    ):
        """Requirement: After image_transfer, QEMU uses the new image on reboot.
    
        Mock mode: transfers random data, verifies reboot started and
        image_get_info returns correct SHA256.
    
        Emulated mode: tests good→bad→good flow:
        1. Transfer real boot image, reboot, verify SHA256
        2. Transfer random (bad) data, verify different SHA256 is active
        3. Transfer real image again (cache hit), verify original SHA256 restored
        """
        base_url = qemu_container["base_url"]
        api_key = qemu_container["api_key"]
    
        if is_mock_mode():
            # Mock mode: simple transfer + reboot
            content = os.urandom(2048)
            sha256 = hashlib.sha256(content).hexdigest()
            with tempfile.NamedTemporaryFile(suffix=".img", delete=False) as f:
                f.write(content)
                local_path = f.name
    
            try:
                s3_key = f"test-reboot-{sha256[:8]}.img"
>               await upload_to_minio(minio_service, local_path, s3_key)

tests/e2e/qemu_server/test_image_transfer.py:251: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

minio_info = {'access_key': 'minioadmin', 'bucket': 'test-images', 'host_url': 'http://localhost:51439', 'internal_url': 'http://lab-mcp-minio-2c5c52a5:9000', ...}
local_path = '/tmp/tmpj0wt1kke.img', s3_key = 'test-reboot-43a19124.img'

    async def upload_to_minio(
        minio_info: dict,
        local_path: str,
        s3_key: str,
    ) -> None:
        """Upload file to MinIO using aws s3 cp CLI via localhost endpoint."""
        env = {
            **os.environ,
            "AWS_ACCESS_KEY_ID": minio_info["access_key"],
            "AWS_SECRET_ACCESS_KEY": minio_info["secret_key"],
            "AWS_DEFAULT_REGION": "us-east-1",
        }
        proc = await asyncio.create_subprocess_exec(
            "aws",
            "--endpoint-url",
            minio_info["host_url"],
            "s3",
            "cp",
            local_path,
            f"s3://{minio_info['bucket']}/{s3_key}",
            env=env,
            stdout=asyncio.subprocess.PIPE,
            stderr=asyncio.subprocess.PIPE,
        )
        stdout, stderr = await proc.communicate()
>       assert proc.returncode == 0, f"aws s3 cp failed: {stderr.decode()}"
E       AssertionError: aws s3 cp failed: upload failed: ../../../../../../../../tmp/tmpj0wt1kke.img to s3://test-images/test-reboot-43a19124.img Could not connect to the endpoint URL: "http://localhost:51439/test-images/test-reboot-43a19124.img"
E         
E       assert 1 == 0
E        +  where 1 = <Process 2381513>.returncode

tests/e2e/conftest.py:1346: AssertionError
