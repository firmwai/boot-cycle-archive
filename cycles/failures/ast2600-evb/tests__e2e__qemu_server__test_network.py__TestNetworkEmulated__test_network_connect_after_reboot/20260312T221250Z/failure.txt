self = <tests.e2e.qemu_server.test_network.TestNetworkEmulated object at 0xe45ec4168bf0>
qemu_container = {'api_key': 'test-key', 'base_url': 'http://localhost:35173', 'container': <Container: 6c6143990325>, 'container_name': 'test-qemu-a0560615', ...}

    async def test_network_connect_after_reboot(self, qemu_container):
        """Network can be re-established after reboot."""
        info = qemu_container
        timeout = e2e_timeout(30, 300)
    
        await _boot_and_login(info, timeout=timeout)
    
        # Connect network
        result1 = await call_tool(
            info["base_url"],
            info["api_key"],
            "network_connect",
            {"ssh_username": "root", "ssh_password": "0penBmc"},
            timeout=timeout,
        )
>       assert result1["status"] == "connected"
E       AssertionError: assert 'error' == 'connected'
E         
E         - connected
E         + error

tests/e2e/qemu_server/test_network.py:297: AssertionError
