self = <tests.e2e.qemu_server.test_file_transfer.TestFileTransfer object at 0xefed3a39bb30>
qemu_container = {'api_key': 'test-key', 'base_url': 'http://localhost:58719', 'container': <Container: 077c27e737da>, 'container_name': 'test-qemu-9d2a9094', ...}

    async def test_put_wait_timeout_zero_returns_transfer_id(self, qemu_container):
        """Requirement: file_transfer with wait_timeout=0 returns immediately
        with a transfer_id for async tracking.
        """
        info = qemu_container
        timeout = e2e_timeout(30, 180)
    
>       connect_result = await _boot_login_and_connect(info, timeout=timeout)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/e2e/qemu_server/test_file_transfer.py:319: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/e2e/qemu_server/test_file_transfer.py:74: in _boot_login_and_connect
    await _boot_and_login(info)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

info = {'api_key': 'test-key', 'base_url': 'http://localhost:58719', 'container': <Container: 077c27e737da>, 'container_name': 'test-qemu-9d2a9094', ...}
timeout = 520.0

    async def _boot_and_login(info, timeout=None):
        """Boot target and wait for shell."""
        if timeout is None:
            timeout = e2e_timeout(60, 520)
        base_url = info["base_url"]
        api_key = info["api_key"]
    
        await configure_credentials(base_url, api_key)
    
        await call_tool(
            base_url,
            api_key,
            "target",
            {
                "op": "reboot",
                "start_operation": "login_shell",
                "timeout": e2e_timeout(300, 500),
            },
        )
    
        result = await call_tool(
            base_url,
            api_key,
            "wait_for_event",
            {"event_types": ["shell_ready", "operation_ended"], "timeout": timeout},
            timeout=timeout + 10,
        )
    
        if result.get("status") != "matched":
            pytest.fail(f"Login did not complete within {timeout}s — boot too slow")
        events = result.get("events", [])
        if not any(e.get("event_type") == "shell_ready" for e in events):
>           pytest.fail("Login operation ended without shell_ready — boot stalled")
E           Failed: Login operation ended without shell_ready — boot stalled

tests/e2e/qemu_server/test_file_transfer.py:67: Failed
