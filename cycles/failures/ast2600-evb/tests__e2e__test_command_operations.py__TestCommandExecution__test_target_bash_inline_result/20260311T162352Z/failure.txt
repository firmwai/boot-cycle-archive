self = <tests.e2e.test_command_operations.TestCommandExecution object at 0xf6ea20d2b4a0>
qemu_container = {'api_key': 'test-key', 'base_url': 'http://localhost:54209', 'container': <Container: b33992e94d1a>, 'container_name': 'test-qemu-b38d8ca2', ...}

    async def test_target_bash_inline_result(self, qemu_container):
        """target_bash returns inline result for fast command."""
        base_url = qemu_container["base_url"]
        api_key = qemu_container["api_key"]
    
        # Boot with login
        await call_tool(
            base_url,
            api_key,
            "target_reboot",
            {
                "start_operation": "login_shell",
                "username": "root",
                "password": "0penBmc",
                "timeout": e2e_timeout(300, 500),
            },
        )
    
        # Wait for shell_ready or operation_ended (login may time out under load)
        result = await call_tool(
            base_url,
            api_key,
            "wait_for_event",
            {"event_types": ["shell_ready", "operation_ended"], "timeout": e2e_timeout(60, 520)},
            timeout=e2e_timeout(70, 530),
        )
        assert result.get("status") == "matched", f"Login did not complete: {result}"
        events = result.get("events", [])
        if not any(e.get("event_type") == "shell_ready" for e in events):
            pytest.skip("Login failed under load (boot stalled) — not a bash test issue")
    
        # Confirm shell is actually responsive (not just shell_ready event)
        await wait_for_shell_responsive(base_url, api_key, timeout=e2e_timeout(5, 30))
    
        # Execute a simple command
        result = await call_tool(
            base_url,
            api_key,
            "target_bash",
            {"command": "echo hello_from_test", "wait_timeout": e2e_timeout(30, 60)},
            timeout=e2e_timeout(35.0, 65.0),
        )
    
        # Should get either inline result or running status
        assert result.get("status") in ("completed", "running"), f"Unexpected: {result}"
        assert "command_id" in result
    
        if result["status"] == "completed":
            assert result.get("exit_code") == 0
>           assert "hello_from_test" in result.get("output", "")
E           AssertionError: assert 'hello_from_test' in ''
E            +  where '' = <built-in method get of dict object at 0xf6ea1ff54cc0>('output', '')
E            +    where <built-in method get of dict object at 0xf6ea1ff54cc0> = {'command': 'echo hello_from_test', 'command_id': '3864bd25', 'duration_seconds': 3.7, 'exit_code': 0, ...}.get

tests/e2e/test_command_operations.py:290: AssertionError
