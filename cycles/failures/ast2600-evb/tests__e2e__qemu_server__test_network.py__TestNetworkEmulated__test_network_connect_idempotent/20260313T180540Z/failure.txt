self = <tests.e2e.qemu_server.test_network.TestNetworkEmulated object at 0xf701e7a4b110>
qemu_container = {'api_key': 'test-key', 'base_url': 'http://localhost:41771', 'container': <Container: efad592a148b>, 'container_name': 'test-qemu-db925570', ...}

    async def test_network_connect_idempotent(self, qemu_container):
        """network_connect when already connected returns already_connected."""
        info = qemu_container
        timeout = e2e_timeout(30, 300)
    
        await _boot_and_login(info, timeout=timeout)
    
        # First connect
        result1 = await call_tool(
            info["base_url"],
            info["api_key"],
            "network_connect",
            {},
            timeout=timeout,
        )
>       assert result1["status"] == "connected"
E       AssertionError: assert 'error' == 'connected'
E         
E         - connected
E         + error

tests/e2e/qemu_server/test_network.py:238: AssertionError
